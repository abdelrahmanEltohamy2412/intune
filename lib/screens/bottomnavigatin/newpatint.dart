import 'package:flutter/material.dart';

class AddNewPateint extends StatelessWidget {
  const AddNewPateint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('اااه'),
    );
  }
}
